const {functionName} = require('./main');

// Setup data;
const data = [];

describe('case name', () => {
    test('should be return', () => {
        const expect = "";
        expect(functionName(nums)).toEqual(expect);
    });
})

